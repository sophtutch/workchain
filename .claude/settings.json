{
  "permissions": {
    "allow": [
      "Bash(pytest:*)",
      "Bash(pip:*)",
      "Bash(python:*)",
      "Bash(python3:*)",
      "Bash(find:*)",
      "Bash(cat:*)",
      "Bash(ls:*)",
      "Bash(hatch:*)"
    ],
    "deny": [
      "Bash(rm -rf:*)",
      "Bash(mongodump:*)",
      "Bash(mongorestore:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); p=d.get('tool_response',{}).get('filePath') or d.get('tool_input',{}).get('file_path',''); exit(0 if p.endswith('.py') else 1)\" && hatch fmt 2>/dev/null || true",
            "timeout": 30,
            "statusMessage": "Running hatch fmt..."
          },
          {
            "type": "command",
            "command": "python -c \"import sys,json; d=json.load(sys.stdin); p=d.get('tool_response',{}).get('filePath') or d.get('tool_input',{}).get('file_path',''); exit(0 if p.endswith('.py') else 1)\" && hatch test 2>/dev/null || true",
            "timeout": 120,
            "statusMessage": "Running hatch test..."
          }
        ]
      }
    ]
  }
}
